# Python
__pycache__/
*.pyc
.env
.venv/
*.egg-info/
.ruff_cache/
.pytest_cache/

# Node
node_modules/
dist/
dist-ssr/
*.local

# Rust
target/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# WASM build output
frontend/src/wasm/pkg/

# Logs
*.log
npm-debug.log*
backend/logs/

# Assets (large files)
checkpoints/

# Worktrees
.worktrees/

# Superpowers brainstorm sessions
.superpowers/

# Runtime config
settings.yaml

# Step 5: Files API content-addressed blobs (not source)
backend/data/files/
# local SQLite dev DBs (PG is canonical)
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal
.claude/scheduled_tasks.lock
.gstack/

# Per-run eval output (committed baselines live in baselines/)
backend/tests/evals/compact/latest_report.json
backend/configs/runtime_overrides.json

# DB/config 迁移期的 .env 备份(含密码,绝不提交)
.env.bak*
backend/.env.bak*

# 专用生产检出标记(只在 nous-prod 检出存在,deploy.sh 凭它放行 reset --hard;
# dev 检出不该有 → 误跑 deploy.sh 会被拒)。见 infra/PROD_CHECKOUT.md
.nous-production
# ship.sh 的上线闸门开关(enginectl autodeploy on|off),落在生产检出根目录
.nous-autodeploy-off

# coverage (pytest-cov)
.coverage
backend/.coverage
htmlcov/
coverage.xml
