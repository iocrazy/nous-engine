# nous-engine 内网地址 —— **单一真相源**。换网/换机/重装 tailnet 只改这个文件。
#
# 格式必须同时被 shell `source` 和 systemd `EnvironmentFile=` 接受:
#   KEY=value,等号两边无空格,不要 `export`,不要行尾注释,值里有逗号也不要加引号
#   (systemd 会把引号当值的一部分)。
#
# 消费者(改了这里,这些地方自动跟着变):
#   - infra/systemd/nous-engine-comfyui.service  ExecStart 的 --listen
#   - infra/systemd/enginectl                    status 的内网探测
#   - infra/systemd/install.sh                   收尾健康检查与访问地址
# 一致性由 backend/tests/test_infra_privilege_boundary.py 兜住。

# 本机在 tailnet 里的地址(2026-09-21 从 ZeroTier 10.0.0.10 迁来;tailnet 名 heygo-ubuntu)。
# 换 tailnet 或重装节点后 Tailscale 会重新分配,改这一行即可。
NOUS_TS_HOST=100.124.149.118

# ComfyUI sidecar 的 --listen 地址表(逗号分隔,main.py 对它 split(",") 后每个地址各起
# 一个 TCPSite)。
#
# ⚠️ **安全边界,改之前务必读**:ComfyUI 自身**零鉴权**,而它的节点能读写任意文件、
# 执行自定义代码。所以这里**只能逐个点名网卡**:
#   127.0.0.1        → 后端桥(NOUS_COMFY_URL)+ 本机浏览器。**摘掉桥就 Connection refused**
#   ${NOUS_TS_HOST}  → Tailscale,用户从别的机器开 ComfyUI 界面走这个
# LAN(192.168.8.x)、docker0/br-*(172.x)、mihomo 的 Meta(198.18.x)一个口都不开。
#
# **绝不能写成 0.0.0.0 / :: / * / 留空** —— 那等于把这台机器的 RCE 交给能路由到它的
# 任何人。test_infra_privilege_boundary.py 会逐个地址判定,写了通配符直接红。
# 注意 systemd 不做嵌套展开,所以这里必须写完整 IP,不能写 ${NOUS_TS_HOST};
# 两者是否一致由测试兜住。
NOUS_COMFY_LISTEN=127.0.0.1,100.124.149.118
